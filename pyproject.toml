[project]
name = "local-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.27.1",
    "pygame-ce>=2.5.7",
    "pytest>=9.0.3",
    "requests",
]
